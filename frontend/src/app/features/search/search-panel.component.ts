import { CommonModule } from '@angular/common';import { Component, EventEmitter, Input, OnDestroy, Output, inject, signal } from '@angular/core';import { FormBuilder, ReactiveFormsModule } from '@angular/forms';import { Subject, Subscription, catchError, debounceTime, EMPTY, switchMap, tap } from 'rxjs';import { SearchHit, SearchService } from './search.service';
@Component({selector:'app-search-panel',standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<div class="global-search" [formGroup]="form"><input type="search" formControlName="query" placeholder="Search messages and people" aria-label="Search" (input)="search()">@if(open()){<div class="search-results">@if(loading()){<p>Searching...</p>}@for(hit of results();track hit.type+hit.id){<button type="button" (click)="choose(hit)"><strong>{{hit.title}}</strong><span>{{hit.contextName}}</span><small>{{hit.snippet}}</small></button>}@if(!loading()&&results().length===0){<p>No accessible results found.</p>}</div>}</div>`})
export class SearchPanelComponent implements OnDestroy{private readonly service=inject(SearchService);private readonly requests=new Subject<string>();private readonly subscription:Subscription;@Input() workspaceId:number|null=null;@Output() selected=new EventEmitter<SearchHit>();readonly results=signal<SearchHit[]>([]);readonly open=signal(false);readonly loading=signal(false);readonly form=inject(FormBuilder).nonNullable.group({query:['']});constructor(){this.subscription=this.requests.pipe(debounceTime(300),tap(()=>this.loading.set(true)),switchMap(query=>this.service.search(query,this.workspaceId).pipe(catchError(()=>{this.loading.set(false);return EMPTY;})))).subscribe(response=>{this.results.set(response.results);this.open.set(true);this.loading.set(false);});}search():void{const query=this.form.getRawValue().query.trim();if(query.length<2){this.results.set([]);this.open.set(false);return;}this.requests.next(query);}choose(hit:SearchHit):void{this.open.set(false);this.selected.emit(hit);}ngOnDestroy():void{this.subscription.unsubscribe();}}
